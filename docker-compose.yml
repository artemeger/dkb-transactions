services:
  electron-builder:
    build:
      context: .
      dockerfile: Dockerfile
    user: "${UID:-1000}:${GID:-1000}"
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - HOME=/home/node
      - DISPLAY=:99
      - EW_SKIP_CODE_SIGNING=true
